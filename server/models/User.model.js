// User data schema model stub
export class User {
  constructor({ id, name, email, role, status }) {
    this.id = id;
    this.name = name;
    this.email = email;
    this.role = role;
    this.status = status;
  }
}
